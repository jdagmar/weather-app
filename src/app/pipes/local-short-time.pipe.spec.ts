import { LocalShortTimePipe } from './local-short-time.pipe';

describe('LocalShortTimePipe', () => {
  it('create an instance', () => {
    const pipe = new LocalShortTimePipe();
    expect(pipe).toBeTruthy();
  });
});
