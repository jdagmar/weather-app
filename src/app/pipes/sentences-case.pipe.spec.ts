import { SentencesCasePipe } from './sentences-case.pipe';

describe('SentencesCasePipe', () => {
  it('create an instance', () => {
    const pipe = new SentencesCasePipe();
    expect(pipe).toBeTruthy();
  });
});
